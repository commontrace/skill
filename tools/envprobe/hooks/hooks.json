{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python >/dev/null 2>&1; then exec python \"${CLAUDE_PLUGIN_ROOT}/hooks/dump_env.py\"; else exec python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/dump_env.py\"; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
